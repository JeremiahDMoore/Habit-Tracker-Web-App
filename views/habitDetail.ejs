<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/style.css">
    <title>Habit Tracker</title>
    <link rel="stylesheet" href="css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">

</head>
<body>
    <span class="titleBar">
        <% if (left === 0 ){ %>
            <h3 class="title">Daily Habit Tracker</h3>
            <h4 class="title">Start making positive changes in your life by tracking habits 🏆<br>Set a number of days 📅 and keep track of your daily streak 🔥<br>Hold yourself accountable, consistency is key! 🗝️</h4>    
            <%  } else if (left === 1 ){ %> 
            <h3 class="title"><%= user.userName %> is tracking <%= left %> habit</h3>
            <%  } else { %> 
            <h3 class="title"><%= user.userName %> is tracking <%= left %> habits</h3>
            <% } %>
            <a href="/logout" class="link">Logout</a>
    </span>
    <ol>
        <% var i = 0 %>
    <% todos.forEach( el => { %>
        <% i += 1 %>
            <li class='todoItem rapper' data-id='<%=el._id%>'>
                <span class="goal"><%= el.todo %> for <%= el.totaldays %> days in a row</span>
                <span class="motive"><%= el.motivation %></span>
                <span class="setDate"><%= el.startdate.toLocaleDateString('en-us', { weekday:"long", year:"numeric", month:"short", day:"numeric"}) %></span>
                <% if (el.streak >= el.totaldays) { %>
                    <span class="streak">You have completed <%= el.totaldays %> days in a row!</span>
                    <%  } else { %>                
                    <span class='addLike'><%= el.streak %></span>
                    <span id="add" class='fa fa-plus-circle plusOne'></span>
                    <% } %>
                    <span id="delete" class='fa fa-trash del'><% i %></span>  

            </li>
    <% }) %>    
    </ol>


    <article class="bar">
        <br>
        <span class="title">Add a new Habit</span>    
    <form action="/todos/createTodo" method='POST'>
        <input type="text" placeholder="Enter Goal" name='todoItem'>    
        <input type="text" placeholder="Motivation" name='motivation'>
        <input type="number" placeholder="# of days" name='totalDays'><br>
        <!-- start date: <input type="date" placeholder="Start Date" name='startDate'> -->
        <input type="submit" class="btn">
    </form>    
</article>
    <script src="js/main.js"></script>
</body>
</html>